/**
 * Node and card rendering size constants
 */
export const NODE_SIZES = {
    CARD_BASE_SIZE: 25,        // Base size for images
    CIRCLE_RADIUS: 5,     // Radius for circle nodes
    CARD_SCALE: 0.8,      // Scale factor for card images
    CARD_ASPECT: 63/88,    // MTG card aspect ratio
    RANK_MIN_SIZE: 3,     // Minimum size for ranked nodes
    RANK_MAX_SIZE: 6     // Maximum size for ranked nodes
  };
  
  /**
   * Text rendering constants
   */
  export const TEXT_SIZES = {
    MAX_FONT_SIZE: 16,
    BASE_FONT_SIZE: 12,
    LINE_HEIGHT: 1.2,
    LABEL_PADDING: 2,
    MAX_LABEL_WIDTH: 100
  };